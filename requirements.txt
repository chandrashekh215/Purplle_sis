# Core API
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0
websockets==13.1

# MongoDB (replaces Redis + TimescaleDB + Kafka)
motor==3.6.0          # async MongoDB driver
pymongo==4.9.1        # sync MongoDB driver (compatible with motor 3.6.0)

# Computer vision
opencv-python-headless==4.10.0.84
numpy==1.26.4

# Observability
prometheus-fastapi-instrumentator==7.0.0
structlog==24.4.0

# Testing
pytest==8.3.2
pytest-asyncio==0.24.0
httpx==0.27.2
